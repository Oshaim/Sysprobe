from setuptools import setup, find_packages

setup(
    name="sysprobe",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.6",
    description="System probe utilities for filesystem monitoring and kernel tracing",
)
